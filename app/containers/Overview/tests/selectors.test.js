// import { fromJS } from 'immutable';
// import { makeSelectOverviewDomain } from '../selectors';

// const selector = makeSelectOverviewDomain();

describe('makeSelectOverviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
