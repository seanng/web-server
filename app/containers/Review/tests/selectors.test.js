// import { fromJS } from 'immutable';
// import { makeSelectReviewDomain } from '../selectors';

// const selector = makeSelectReviewDomain();

describe('makeSelectReviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
