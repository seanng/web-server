// import { fromJS } from 'immutable';
// import { makeSelectAccountDomain } from '../selectors';

// const selector = makeSelectAccountDomain();

describe('makeSelectAccountDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
